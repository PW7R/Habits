import SwiftUI

enum UIConstants {
    // Height used to inset page content so it doesn't sit under the custom bottom tab bar.
    // Keep this in sync with BottomTabBarWithBinding paddings and height.
    static let bottomTabBarInset: CGFloat = 92
}
